package net.onedsix.registry;

/**
 * Convenience interface for accessing, modifying, and most importantly adding to the registry.
 * */
public interface IRegistryManager {
	/**
	 * Called when the registry is ready to
	 * */
	void registryReady();

}
